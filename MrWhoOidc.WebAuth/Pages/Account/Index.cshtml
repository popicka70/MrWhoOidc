@page "/account"
@inject MrWhoOidc.Auth.MultiTenancy.ITenantAccessor TenantAccessor
@inject Microsoft.Extensions.Options.IOptions<MrWhoOidc.Auth.MultiTenancy.MultiTenancyOptions> MultiTenancyOptions
@model MrWhoOidc.WebAuth.Pages.Account.IndexModel
@{
    ViewData["Title"] = "My Account";
    ViewData["ActiveAccountTab"] = "index";
    var currentTenant = TenantAccessor.CurrentTenant;
    var tenantPrefix = currentTenant != null && MultiTenancyOptions.Value.Enabled 
        ? $"/t/{currentTenant.Slug}" 
        : "";
}

<h1 class="mb-3">My Account</h1>
<partial name="~/Pages/Account/_AccountTabs.cshtml" />

<div class="row g-4">
    <!-- Profile Card -->
    <div class="col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="bi bi-person-circle text-primary"></i> Profile
                </h5>
                <dl class="mb-3">
                    <dt class="small text-muted">Name</dt>
                    <dd class="mb-2">@(Model.Name ?? "(not set)")</dd>
                    
                    <dt class="small text-muted">Username</dt>
                    <dd class="mb-2"><code>@Model.Username</code></dd>
                    
                    <dt class="small text-muted">Email</dt>
                    <dd class="mb-0">
                        @Model.Email
                        @if (Model.EmailVerified)
                        {
                            <span class="badge bg-success ms-1"><i class="bi bi-check-circle"></i> Verified</span>
                        }
                        else
                        {
                            <span class="badge bg-warning ms-1"><i class="bi bi-exclamation-circle"></i> Not Verified</span>
                        }
                    </dd>
                </dl>
                <a href="@(tenantPrefix)/account/profile" class="btn btn-sm btn-outline-primary">
                    <i class="bi bi-pencil me-1"></i>Edit Profile
                </a>
            </div>
            <div class="card-footer text-muted small">
                <i class="bi bi-calendar me-1"></i>Member since @Model.AccountCreatedAt.ToString("MMM d, yyyy")
            </div>
        </div>
    </div>

    <!-- Security Card -->
    <div class="col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="bi bi-shield-lock @(Model.MfaEnabled ? "text-success" : "text-warning")"></i> Security
                </h5>
                <dl class="mb-3">
                    <dt class="small text-muted">Multi-Factor Authentication</dt>
                    <dd class="mb-2">
                        @if (Model.MfaEnabled)
                        {
                            <span class="badge bg-success">
                                <i class="bi bi-check-circle"></i> Enabled
                            </span>
                        }
                        else
                        {
                            <span class="badge bg-warning">
                                <i class="bi bi-exclamation-triangle"></i> Disabled
                            </span>
                        }
                    </dd>
                    
                    <dt class="small text-muted">Security Keys (WebAuthn)</dt>
                    <dd class="mb-2">
                        @if (Model.WebAuthnCredentialsCount > 0)
                        {
                            <span class="badge bg-success">
                                <i class="bi bi-shield-check"></i> @Model.WebAuthnCredentialsCount key@(Model.WebAuthnCredentialsCount == 1 ? "" : "s")
                            </span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">
                                <i class="bi bi-shield"></i> None
                            </span>
                        }
                    </dd>
                    
                    <dt class="small text-muted">Password</dt>
                    <dd class="mb-0">
                        <i class="bi bi-key"></i> •••••••••
                    </dd>
                </dl>
                <div class="d-flex gap-2 flex-wrap">
                    <a href="@(tenantPrefix)/mfa" class="btn btn-sm btn-outline-primary">
                        <i class="bi bi-shield me-1"></i>MFA
                    </a>
                    <a href="@(tenantPrefix)/account/web-authn" class="btn btn-sm btn-outline-primary">
                        <i class="bi bi-shield-check me-1"></i>Keys
                    </a>
                    <a href="@(tenantPrefix)/password" class="btn btn-sm btn-outline-secondary">
                        <i class="bi bi-key me-1"></i>Password
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Sessions Card -->
    <div class="col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="bi bi-phone text-info"></i> Active Sessions
                </h5>
                <div class="display-6 my-3">@Model.ActiveSessionsCount</div>
                <p class="card-text text-muted small mb-3">
                    Active devices with access to your account
                </p>
                <a href="@(tenantPrefix)/account/sessions" class="btn btn-sm btn-outline-primary">
                    <i class="bi bi-eye me-1"></i>View All Sessions
                </a>
            </div>
        </div>
    </div>

    <!-- Consents Card -->
    <div class="col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="bi bi-check2-square text-success"></i> Authorized Apps
                </h5>
                <div class="display-6 my-3">@Model.ActiveConsentsCount</div>
                <p class="card-text text-muted small mb-3">
                    Applications with access to your data
                </p>
                <a href="@(tenantPrefix)/account/consents" class="btn btn-sm btn-outline-primary">
                    <i class="bi bi-eye me-1"></i>Manage Consents
                </a>
            </div>
        </div>
    </div>

    <!-- Linked Accounts Card -->
    <div class="col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="bi bi-link-45deg text-primary"></i> Linked Accounts
                </h5>
                <div class="display-6 my-3">@Model.LinkedAccountsCount</div>
                <p class="card-text text-muted small mb-3">
                    External identities linked to your account
                </p>
                <a href="@(tenantPrefix)/account/linked-accounts" class="btn btn-sm btn-outline-primary">
                    <i class="bi bi-eye me-1"></i>View Linked Accounts
                </a>
            </div>
        </div>
    </div>

    <!-- Alternative Emails Card -->
    <div class="col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="bi bi-envelope text-secondary"></i> Alternative Emails
                </h5>
                <div class="display-6 my-3">@Model.AlternativeEmailsCount</div>
                <p class="card-text text-muted small mb-3">
                    Additional email addresses for your account
                </p>
                <a href="@(tenantPrefix)/account/emails" class="btn btn-sm btn-outline-primary">
                    <i class="bi bi-eye me-1"></i>Manage Emails
                </a>
            </div>
        </div>
    </div>
</div>

<div class="alert alert-info mt-4" role="alert">
    <h5 class="alert-heading"><i class="bi bi-info-circle me-2"></i>Account Management</h5>
    <p class="mb-0">
        Use the tabs above or cards to manage your profile, security settings, active sessions, authorized applications, and more.
        If you need help, contact your administrator.
    </p>
</div>
