@page
@inject MrWhoOidc.Auth.MultiTenancy.ITenantAccessor TenantAccessor
@inject MrWhoOidc.Auth.MultiTenancy.IMultiTenancyOptions MultiTenancyOptions
@model IndexModel
@{
    ViewData["Title"] = "Home page";
    // Show navbar for authenticated users, hide for unauthenticated users
    ViewData["HideNavbar"] = !(User?.Identity?.IsAuthenticated ?? false);
    var currentTenant = TenantAccessor.CurrentTenant;
    var tenantPrefix = currentTenant != null && MultiTenancyOptions.Enabled 
        ? $"/t/{currentTenant.Slug}" 
        : "";
}

<div class="text-center">
    <h1 class="display-4 mb-3">Welcome to MrWho OIDC</h1>
    <p class="lead text-muted mb-4">Modern OpenID Connect Authentication Server</p>
    
    <img src="/images/Logo.jpeg" asp-append-version="true" class="img-fluid mt-3 mb-4 home-logo" alt="Decorative shape" />

    @if (User?.Identity?.IsAuthenticated ?? false)
    {
        <div class="alert alert-success mt-4" role="alert">
            <i class="ph-fill ph-check-circle icon-sm me-2"></i>
            You are signed in as <strong>@User.Identity!.Name</strong>
        </div>
        <div class="mt-4 d-flex gap-3 justify-content-center flex-wrap">
            <a class="btn btn-primary" href="@(tenantPrefix)/Account">
                <i class="ph ph-squares-four"></i> Dashboard
            </a>
            <a class="btn btn-outline-secondary" href="/.well-known/openid-configuration" target="_blank">
                <i class="ph ph-file-code"></i> OIDC Discovery
            </a>
        </div>
    }
    else
    {
        <div class="mt-4 d-flex gap-3 justify-content-center flex-wrap">
            <a class="btn btn-primary btn-lg" asp-page="/DiscoverTenant">
                <i class="ph ph-sign-in"></i> Log in
            </a>
            <a class="btn btn-outline-secondary btn-lg" href="@(tenantPrefix)/Registrations">
                <i class="ph ph-user-plus"></i> Register
            </a>
        </div>
    }
</div>
